httpx>=0.27,<1.0
realtime>=2.0,<3.0
python-dotenv>=1.0,<2.0

# The poller. psycopg rather than PostgREST, because for update skip locked
# has no REST equivalent. The binary extra avoids needing libpq-dev.
psycopg[binary,pool]>=3.1,<4.0
feedparser>=6.0,<7.0

# Web push delivery. Encryption lives here rather than in a Vercel
# function, which is what keeps the serverless side dependency free.
pywebpush>=2.0,<3.0

# Phase 6, the stream listeners
# websockets>=12.0,<14.0
